using DTOMaker.SrcGen.Core;
using Microsoft.CodeAnalysis;

namespace DTOMaker.SrcGen.CSPoco
{
    internal sealed class CSPocoDomain : TargetDomain
    {
        public CSPocoDomain(string name, Location location) : base(name, location) { }
    }
}
